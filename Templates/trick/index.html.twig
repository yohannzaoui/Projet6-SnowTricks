{% extends 'base.html.twig' %}
{% block title %}{{ trick.name }}{% endblock %}

{% block body %}
    <div class="container">
        <br>
        <h1>{{ trick.name }}</h1>
        <div class="row">
            <div class="col-lg-10 col-md-6 mx-auto">
                <img src="{{trick.image}}" class="rounded mx-auto d-block img-fluid" alt="Responsive image">
                <br>
                <div class="alert alert-dismissible alert-secondary">
                    <p>{{ trick.description }}</p>
                    <p class="italicgrey">Ajouté le {{ trick.createdAt| date("d/m/Y") }} {% if trick.updatedAt %} - Modifié le {{ trick.updatedAt | date("d/m/Y") }} {% endif %}</p>
                </div>
            </div>
        </div>
        
        <hr>
        {% if app.user %}
        <div class="col-lg-10 col-md-6 mx-auto">
        <h3>Ecrire un commentaire</h3>
            {% form_theme form 'theme/comment_type.html.twig' %}
            {{form(form)}}
        </div>
        {% else %}
            <p class="p_center">Vous devez être connecté pour commenter.</p>
            <p class="p_center"><a class="italic" href="{{path('login')}}">Connectez-vous</a> ou <a class="italic" href="{{path('register')}}">créez un compte</a>
        {% endif %}
        
        <hr>
        <div class="col-lg-10 col-md-12 mx-auto">
            <h3>
                {% if trick.comment | length == 0 %} 
                    <h4 class="p_center">Aucun commentaire pour ce Trick. Soyez le premier à commenter.</h4> 
                {% elseif trick.comment | length == 1 %}
                    {{trick.comment | length}} Commentaire
                {% else %}
                    {{trick.comment | length}} Commentaires
                {% endif %}
            </h3>

            {% for comment in trick.comment %}
                <div class="alert alert-dismissible alert-secondary">
                    <p>{{ comment.pseudo }} le {{ comment.createdAt | date("d/m/Y à H:i:s") }}</p>
                    <p>A écrit : {{ comment.message }}</p>
                </div>
            {% endfor %}
        </div>
    </div>
</div>

{% endblock body %}