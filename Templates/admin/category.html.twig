{% extends 'base.html.twig' %}
{% block title %}Administration{% endblock %}

{% block body %}
    <div class="container">
        <br>
            <h1>Gestion des catégories</h1>
            <div class="row">
                <div class="col-lg-6 col-md-10 mx-auto">
                    {% form_theme form 'theme/category_type.html.twig' %}
                    {{ form(form)}}
                    </div>
                    <div class="col-lg-6 col-md-10 mx-auto">
                    <h4>Liste des catégories</h4>
                    {% for categorylist in categoryslist %}
                        <p>{{categorylist.name}} |
                            <a href="{{path('delcategory', {'id':categorylist.id})}}"><i class="fas fa-trash-alt"></i></a> |
                            <a href="{#{{path('editcategory', {'id':categorylist.id})}}#}"><i class="fas fa-edit"></i></a>
                        </p>
                    {% endfor %}
                    </div>
                </div>
            </div>
    </div>

{% endblock body %}