<header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <a class="navbar-brand" href="{{path('home')}}"><div class="brandcolor">Snow Tricks</div></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor02">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="{{path('home')}}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            {% if app.user %}
            <li class="nav-item">
              <a class="nav-link" href="{{path('addtrick')}}">Ajouter un Trick</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('profil')}}">Gestion de mon profil</a>
            </li>
            {% endif %}
            {% if not app.user %}
            <li class="nav-item">
              <a class="nav-link" href="{{path('register')}}">Inscription</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{path('login')}}">Connexion</a>
            </li>
            {% else %}
            <li class="nav-item">
              <a style="color:#FB9C04" class="nav-link" href="{{path('logout')}}">Déconnexion</a>
            </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <div class="dropdown">
                        <button class="btn btn-danger dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Administration
                        </button>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                            <a class="dropdown-item" href="{{path('category')}}">Gestion des catégories</a>
                            <a class="dropdown-item" href="{{path('allUsers')}}">Gestion des membres</a>
                        </div>
                    </div>
                {% endif %}
            {% endif %}
          </ul>
          {% if app.user %}
          <span class="navbar-text">
            Bienvenue {{ app.user.getUsername }} !<img src="{{asset("uploads/images/" ~ app.user.profilImage.getImage)}}"/>
          </span>
          {% endif %}
        </div>
      </nav>
      </header>