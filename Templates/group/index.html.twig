{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <br>
            <h1>Gestion des groupes</h1>
            <div class="row">
                <div class="col-lg-6 col-md-10 mx-auto">
                    {{ form_start(form) }}
                        {{ form_row(form.name, {'label':'Nom du groupe','attr':{'placeholder': 'Entrez le nom du groupe'}}) }}
                        {{ form_row(form.description, {'label':'Description du groupe'})}}
                        <p>
                            <button type="submit" class="btn btn-success">Ajouter le groupe</button> 
                            <button type="reset" class="btn btn-danger">Effacer</button>
                        </p>
                    {{ form_end(form)}}
                </div>
            </div>
    </div>

{% endblock body %}