{% extends 'base.html.twig' %}

{% block body %}

    <br>
    <div class="container">
    
    {% if app.user %}

        <h1>Ajouter un Trick</h1>

    

    {% else %}

        <p>Vous n'êtes pas connecter<p>
        <p><a href="{{path('login')}}">Se connecter</a></p>

    {% endif %}
    </div>
{% endblock body %}
